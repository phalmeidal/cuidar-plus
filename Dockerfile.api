FROM node:20-alpine

WORKDIR /app

COPY package.json package-lock.json ./
RUN npm ci

COPY api ./api
COPY scripts ./scripts

RUN npm run db:generate

EXPOSE 3001

CMD ["sh", "-c", "npm run db:deploy && npm run db:seed && npm run start:api"]
